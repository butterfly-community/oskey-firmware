diff --git a/include/zephyr/authentication/fido2/fido2_up.h b/include/zephyr/authentication/fido2/fido2_up.h
index 721270e7f16..31546e4103f 100644
--- a/include/zephyr/authentication/fido2/fido2_up.h
+++ b/include/zephyr/authentication/fido2/fido2_up.h
@@ -24,6 +24,12 @@
 extern "C" {
 #endif
 
+#if defined(CONFIG_FIDO2_UP_CUSTOM)
+/** Provide request details to the custom user-presence backend. */
+void fido2_up_set_request(uint8_t command, const char *rp_id, const uint8_t *user_id,
+			  size_t user_id_len, const char *user_name, const char *user_display_name);
+#endif
+
 /**
  * @brief Check for user presence.
  *
diff --git a/subsys/authentication/fido2/Kconfig b/subsys/authentication/fido2/Kconfig
index 4dc91b97287..bafd4614787 100644
--- a/subsys/authentication/fido2/Kconfig
+++ b/subsys/authentication/fido2/Kconfig
@@ -6,13 +6,13 @@ menuconfig FIDO2
 	select ZCBOR
 	select ZCBOR_CANONICAL
 	select PSA_CRYPTO
-	select SECURE_STORAGE if !BUILD_WITH_TFM
+	select SECURE_STORAGE if !BUILD_WITH_TFM && !FIDO2_CREDENTIALS_CUSTOM
 	select PSA_WANT_KEY_TYPE_ECC_KEY_PAIR_GENERATE
 	select PSA_WANT_KEY_TYPE_ECC_PUBLIC_KEY
 	select PSA_WANT_ALG_ECDSA
 	select PSA_WANT_ALG_ECDH
 	select PSA_WANT_ALG_SHA_256
-	select PSA_WANT_ALG_GCM
+	select PSA_WANT_ALG_GCM if !FIDO2_CREDENTIALS_CUSTOM
 	select PSA_WANT_ALG_HMAC
 	select PSA_WANT_ALG_CBC_NO_PADDING
 	select PSA_WANT_ECC_SECP_R1_256
@@ -33,6 +33,22 @@ config FIDO2_THREAD_PRIORITY
 	int "FIDO2 processing thread priority"
 	default 7
 
+config FIDO2_CREDENTIALS_CUSTOM
+	bool "Application-provided credentials"
+	depends on FIDO2_STORAGE_SETTINGS
+	depends on FIDO2_ATTESTATION_NONE
+	help
+	  Use application-provided credential creation, validation, and signing.
+	  This is intended for applications that derive credentials from an external key hierarchy.
+
+config FIDO2_CREDENTIALS_BACKUP_ELIGIBLE
+	bool "Custom credentials are backup eligible"
+	depends on FIDO2_CREDENTIALS_CUSTOM
+	help
+	  Set the backup eligibility flag for custom credentials. The backup
+	  state remains unset because the authenticator cannot determine whether
+	  the external key hierarchy has actually been backed up.
+
 config FIDO2_CBOR_MAX_SIZE
 	int "Maximum CBOR message buffer size"
 	default 1024
diff --git a/include/zephyr/authentication/fido2/fido2_types.h b/include/zephyr/authentication/fido2/fido2_types.h
index eeeb5f9dfbb..a6654449072 100644
--- a/include/zephyr/authentication/fido2/fido2_types.h
+++ b/include/zephyr/authentication/fido2/fido2_types.h
@@ -108,6 +108,10 @@
 #define AUTH_DATA_FLAG_UP BIT(0)
 /** @brief User Verified (UV) result */
 #define AUTH_DATA_FLAG_UV BIT(2)
+/** @brief Backup Eligible (BE) */
+#define AUTH_DATA_FLAG_BE BIT(3)
+/** @brief Backup State (BS) */
+#define AUTH_DATA_FLAG_BS BIT(4)
 /** @brief Attested credential data included (AT) */
 #define AUTH_DATA_FLAG_AT BIT(6)
 /** @brief Extension data included (ED) */
diff --git a/subsys/authentication/fido2/fido2_clientpin.c b/subsys/authentication/fido2/fido2_clientpin.c
index e33c32eacff..f6626361a89 100644
--- a/subsys/authentication/fido2/fido2_clientpin.c
+++ b/subsys/authentication/fido2/fido2_clientpin.c
@@ -3,6 +3,8 @@
  *
  * SPDX-License-Identifier: Apache-2.0
  */
+#define _POSIX_C_SOURCE 200809L /* Required for strnlen() */
+
 #include <zephyr/logging/log.h>
 #include <zephyr/authentication/fido2/fido2_storage.h>
 #include <zephyr/random/random.h>
diff --git a/subsys/authentication/fido2/fido2_core.c b/subsys/authentication/fido2/fido2_core.c
index 4b767bdc685..727da45be1f 100644
--- a/subsys/authentication/fido2/fido2_core.c
+++ b/subsys/authentication/fido2/fido2_core.c
@@ -12,6 +12,7 @@
 #include <zephyr/logging/log.h>
 #include <zephyr/authentication/fido2/fido2.h>
 #include <zephyr/authentication/fido2/fido2_attestation.h>
+#include <zephyr/authentication/fido2/fido2_credentials.h>
 #include <zephyr/authentication/fido2/fido2_storage.h>
 #include <zephyr/authentication/fido2/fido2_transport.h>
 #include <zephyr/authentication/fido2/fido2_types.h>
@@ -123,6 +124,12 @@ static bool mc_check_exclude_list(void)
 #endif
 			return true;
 		} else if (mc_params.exclude_id_lens[i] == FIDO2_NON_DISCOVERABLE_CRED_ID_SIZE) {
+#if defined(CONFIG_FIDO2_CREDENTIALS_CUSTOM)
+			if (fido2_credentials_validate(mc_params.exclude_ids[i],
+						       mc_credential.rp_id_hash) == 0) {
+				return true;
+			}
+#else
 			uint32_t key_id;
 
 			if (fido2_crypto_unwrap_credential(mc_params.exclude_ids[i],
@@ -132,6 +139,7 @@ static bool mc_check_exclude_list(void)
 			}
 
 			return true;
+#endif
 		}
 	}
 
@@ -140,8 +148,6 @@ static bool mc_check_exclude_list(void)
 
 static int mc_populate_credential(void)
 {
-	int ret;
-
 	strncpy(mc_credential.rp_id, mc_params.rp_id, sizeof(mc_credential.rp_id) - 1);
 	strncpy(mc_credential.rp_name, mc_params.rp_name, sizeof(mc_credential.rp_name) - 1);
 	memcpy(mc_credential.user_id, mc_params.user_id, mc_params.user_id_len);
@@ -163,20 +169,24 @@ static int mc_populate_credential(void)
 #endif
 
+#if !defined(CONFIG_FIDO2_CREDENTIALS_CUSTOM)
+	int ret;
+
 	if (mc_params.resident_key) {
 		mc_credential.id_len = FIDO2_DISCOVERABLE_CRED_ID_SIZE;
 		ret = sys_csrand_get(mc_credential.id, mc_credential.id_len);
 		if (ret) {
 			LOG_ERR("Credential ID csrand failed: %d", ret);
 			return ret;
 		}
 	} else {
 		mc_credential.id_len = FIDO2_NON_DISCOVERABLE_CRED_ID_SIZE;
 		ret = fido2_crypto_wrap_credential(mc_credential.key_id, mc_credential.rp_id_hash,
 						   mc_credential.id);
 		if (ret) {
 			LOG_ERR("Credential ID wrap failed: %d", ret);
 			return ret;
 		}
 	}
+#endif
 
 	return 0;
@@ -201,8 +211,12 @@ static int mc_store_discoverable(void)
 			if (mc_credential.user_id_len == ga_credentials[i].user_id_len &&
 			    memcmp(mc_credential.user_id, ga_credentials[i].user_id,
 				   mc_credential.user_id_len) == 0) {
-				fido2_storage_remove(ga_credentials[i].id,
-						     ga_credentials[i].id_len);
+				ret = fido2_storage_remove(ga_credentials[i].id,
+							   ga_credentials[i].id_len);
+				if (ret) {
+					return ret;
+				}
+
 				break;
 			}
 		}
@@ -288,6 +302,12 @@ handle_make_credential(uint8_t *cbor_in, size_t cbor_in_len, uint8_t *cbor_out,
 		return FIDO2_ERR_INVALID_CBOR;
 	}
 
+#if defined(CONFIG_FIDO2_UP_CUSTOM)
+	fido2_up_set_request(FIDO2_CMD_MAKE_CREDENTIAL, mc_params.rp_id, mc_params.user_id,
+			     mc_params.user_id_len, mc_params.user_name,
+			     mc_params.user_display_name);
+#endif
+
 	/* UP must not be false. */
 	if (mc_params.has_up_option && !mc_params.up) {
 		LOG_WRN("Rejected MakeCredential: options.up cannot be false");
@@ -382,6 +402,15 @@ handle_make_credential(uint8_t *cbor_in, size_t cbor_in_len, uint8_t *cbor_out,
 		return FIDO2_ERR_CREDENTIAL_EXCLUDED;
 	}
 
+#if defined(CONFIG_FIDO2_CREDENTIALS_CUSTOM)
+	ret = fido2_credentials_make(mc_params.rp_id, mc_credential.id, pub_key);
+	if (ret) {
+		LOG_ERR("Credential derivation failed: %d", ret);
+		return FIDO2_ERR_OTHER;
+	}
+	mc_credential.id_len = FIDO2_NON_DISCOVERABLE_CRED_ID_SIZE;
+	pub_key_len = FIDO2_P256_UNCOMPRESSED_KEY_SIZE;
+#else
 	ret = fido2_crypto_generate_keypair(&mc_credential.key_id);
 	if (ret) {
 		LOG_ERR("Keypair generation failed: %d", ret);
@@ -395,6 +425,7 @@ handle_make_credential(uint8_t *cbor_in, size_t cbor_in_len, uint8_t *cbor_out,
 		status = FIDO2_ERR_OTHER;
 		goto cleanup;
 	}
+#endif
 
 	if (pub_key_len != FIDO2_P256_UNCOMPRESSED_KEY_SIZE ||
 	    pub_key[0] != FIDO2_EC_POINT_UNCOMPRESSED) {
@@ -426,6 +456,10 @@ handle_make_credential(uint8_t *cbor_in, size_t cbor_in_len, uint8_t *cbor_out,
 	flags = AUTH_DATA_FLAG_AT | AUTH_DATA_FLAG_UP;
 
+	if (IS_ENABLED(CONFIG_FIDO2_CREDENTIALS_BACKUP_ELIGIBLE)) {
+		flags |= AUTH_DATA_FLAG_BE;
+	}
+
 	if (user_verified) {
 		flags |= AUTH_DATA_FLAG_UV;
 	}
 
@@ -463,7 +498,9 @@ handle_make_credential(uint8_t *cbor_in, size_t cbor_in_len, uint8_t *cbor_out,
 
 cleanup:
 	if (status != FIDO2_OK) {
+#if !defined(CONFIG_FIDO2_CREDENTIALS_CUSTOM)
 		fido2_crypto_destroy_key(mc_credential.key_id);
+#endif
 		return status;
 	}
 
@@ -486,17 +519,23 @@ static int ga_check_allow_list(uint8_t rp_id_hash[FIDO2_SHA256_SIZE], size_t *fo
 				memcpy(&ga_credentials[(*found_count)++], &cred, sizeof(cred));
 			}
 		} else if (ga_params.allow_id_lens[i] == FIDO2_NON_DISCOVERABLE_CRED_ID_SIZE) {
+#if defined(CONFIG_FIDO2_CREDENTIALS_CUSTOM)
+			ret = fido2_credentials_validate(ga_params.allow_ids[i], rp_id_hash);
+			if (!ret && *found_count < CONFIG_FIDO2_MAX_CREDENTIALS) {
+				memset(&cred, 0, sizeof(cred));
+#else
 			uint32_t key_id;
 
 			ret = fido2_crypto_unwrap_credential(ga_params.allow_ids[i], rp_id_hash,
 							     &key_id);
 			if (!ret && *found_count < CONFIG_FIDO2_MAX_CREDENTIALS) {
 				memset(&cred, 0, sizeof(cred));
+				cred.key_id = key_id;
+#endif
 				memcpy(cred.rp_id_hash, rp_id_hash, FIDO2_SHA256_SIZE);
 				memcpy(cred.id, ga_params.allow_ids[i],
 				       FIDO2_NON_DISCOVERABLE_CRED_ID_SIZE);
 				cred.id_len = FIDO2_NON_DISCOVERABLE_CRED_ID_SIZE;
-				cred.key_id = key_id;
 				cred.algorithm = FIDO2_COSE_ES256;
 				cred.discoverable = false;
 				memcpy(&ga_credentials[(*found_count)++], &cred, sizeof(cred));
@@ -521,15 +563,15 @@ static int build_assertion_response(const struct fido2_credential *cred,
 	size_t sig_len;
 	int ret;
 
+	sign_count = 0;
+
-	if (cred->discoverable) {
+	if (cred->discoverable && !IS_ENABLED(CONFIG_FIDO2_CREDENTIALS_BACKUP_ELIGIBLE)) {
 		ret = fido2_storage_sign_count_increment(cred->id, cred->id_len, &sign_count);
 		if (ret) {
 			LOG_ERR("Sign count increment failed: %d", ret);
 			return ret;
 		}
-	} else {
-		sign_count = 0;
 	}
 
 	memcpy(auth_data, rp_id_hash, FIDO2_SHA256_SIZE);
 	auth_data[FIDO2_SHA256_SIZE] = flags;
@@ -542,9 +584,18 @@ static int build_assertion_response(const struct fido2_credential *cred,
 		return ret;
 	}
 
+#if defined(CONFIG_FIDO2_CREDENTIALS_CUSTOM)
+	ret = fido2_credentials_sign(cred->id, rp_id_hash, sig_input_hash, sig, sizeof(sig),
+				     &sig_len);
+#else
 	ret = fido2_crypto_sign(cred->key_id, sig_input_hash, sig, sizeof(sig), &sig_len);
+#endif
 	if (ret) {
+#if defined(CONFIG_FIDO2_CREDENTIALS_CUSTOM)
+		LOG_ERR("Assertion sign failed: %d", ret);
+#else
 		LOG_ERR("Assertion sign failed key_id=0x%08x: %d", (unsigned int)cred->key_id, ret);
+#endif
 		return ret;
 	}
 
@@ -615,6 +666,12 @@ static enum fido2_status handle_get_assertion(uint8_t *cbor_in, size_t cbor_in_l
 		return FIDO2_ERR_NO_CREDENTIALS;
 	}
 
+#if defined(CONFIG_FIDO2_UP_CUSTOM)
+	fido2_up_set_request(FIDO2_CMD_GET_ASSERTION, ga_params.rp_id, ga_credentials[0].user_id,
+			     ga_credentials[0].user_id_len, ga_credentials[0].user_name,
+			     ga_credentials[0].user_display_name);
+#endif
+
 	if (ga_params.has_pin_uv_auth_param) {
 		if (ga_params.pin_uv_auth_param_len == 0) {
 			set_runtime_state(FIDO2_RUNTIME_STATE_WAITING_USER_PRESENCE);
@@ -671,6 +728,10 @@ static enum fido2_status handle_get_assertion(uint8_t *cbor_in, size_t cbor_in_l
 		flags = AUTH_DATA_FLAG_UP;
 	}
 
+	if (IS_ENABLED(CONFIG_FIDO2_CREDENTIALS_BACKUP_ELIGIBLE)) {
+		flags |= AUTH_DATA_FLAG_BE;
+	}
+
 	if (user_verified) {
 		flags |= AUTH_DATA_FLAG_UV;
 	}
@@ -873,6 +931,10 @@ static enum fido2_status handle_selection(const struct fido2_transport *transpor
 {
 	int ret;
 
+#if defined(CONFIG_FIDO2_UP_CUSTOM)
+	fido2_up_set_request(FIDO2_CMD_SELECTION, NULL, NULL, 0, NULL, NULL);
+#endif
+
 	set_runtime_state(FIDO2_RUNTIME_STATE_WAITING_USER_PRESENCE);
 	notify_wire(transport, cid, FIDO2_WIRE_STATUS_UP_NEEDED);
 
diff --git a/subsys/authentication/fido2/fido2_crypto.c b/subsys/authentication/fido2/fido2_crypto.c
index 7685f447b48..cb504d307c3 100644
--- a/subsys/authentication/fido2/fido2_crypto.c
+++ b/subsys/authentication/fido2/fido2_crypto.c
@@ -35,6 +35,7 @@ LOG_MODULE_DECLARE(fido2, CONFIG_FIDO2_LOG_LEVEL);
 /* tag (16) */
 #define FIDO2_WRAP_TAG_SIZE     PSA_AEAD_TAG_LENGTH(PSA_KEY_TYPE_AES, 256, PSA_ALG_GCM)
 
+#if !defined(CONFIG_FIDO2_CREDENTIALS_CUSTOM)
 static psa_status_t ensure_wrap_key(void)
 {
 	psa_key_attributes_t attr = PSA_KEY_ATTRIBUTES_INIT;
@@ -70,6 +71,7 @@ static psa_status_t ensure_wrap_key(void)
 
 	return status;
 }
+#endif
 
 static psa_status_t ecdsa_int32_to_der(const uint8_t in[32], uint8_t *out, size_t out_size,
 				       size_t *out_len)
@@ -155,11 +157,13 @@ int fido2_crypto_init(void)
 		return status;
 	}
 
+#if !defined(CONFIG_FIDO2_CREDENTIALS_CUSTOM)
 	status = ensure_wrap_key();
 	if (status != PSA_SUCCESS) {
 		LOG_ERR("Credential wrap key init failed: %d", status);
 		return status;
 	}
+#endif
 
 	return status;
 }
diff --git a/subsys/authentication/fido2/storage/fido2_storage.c b/subsys/authentication/fido2/storage/fido2_storage.c
index c3ffc3ffc48..6980feb8e71 100644
--- a/subsys/authentication/fido2/storage/fido2_storage.c
+++ b/subsys/authentication/fido2/storage/fido2_storage.c
@@ -50,9 +50,11 @@ int fido2_storage_remove(const uint8_t *cred_id, size_t cred_id_len)
 		return ret;
 	}
 
-	ret = destroy_credential_key(cred.key_id);
+	if (cred.key_id == PSA_KEY_ID_NULL) {
+		return 0;
+	}
 
-	return ret;
+	return destroy_credential_key(cred.key_id);
 }
 
 int fido2_storage_find_by_rp(const uint8_t rp_id_hash[FIDO2_SHA256_SIZE],
diff --git a/include/zephyr/authentication/fido2/fido2_credentials.h b/include/zephyr/authentication/fido2/fido2_credentials.h
new file mode 100644
index 00000000000..ab2b23400f9
--- /dev/null
+++ b/include/zephyr/authentication/fido2/fido2_credentials.h
@@ -0,0 +1,30 @@
+/*
+ * SPDX-License-Identifier: Apache-2.0
+ */
+
+#ifndef ZEPHYR_INCLUDE_AUTHENTICATION_FIDO2_FIDO2_CREDENTIALS_H_
+#define ZEPHYR_INCLUDE_AUTHENTICATION_FIDO2_FIDO2_CREDENTIALS_H_
+
+#include <zephyr/authentication/fido2/fido2_types.h>
+
+#ifdef __cplusplus
+extern "C" {
+#endif
+
+int fido2_credentials_make(const char *rp_id,
+			   uint8_t credential_id[FIDO2_NON_DISCOVERABLE_CRED_ID_SIZE],
+			   uint8_t public_key[FIDO2_P256_UNCOMPRESSED_KEY_SIZE]);
+
+int fido2_credentials_validate(const uint8_t credential_id[FIDO2_NON_DISCOVERABLE_CRED_ID_SIZE],
+			       const uint8_t rp_id_hash[FIDO2_SHA256_SIZE]);
+
+int fido2_credentials_sign(const uint8_t credential_id[FIDO2_NON_DISCOVERABLE_CRED_ID_SIZE],
+			   const uint8_t rp_id_hash[FIDO2_SHA256_SIZE],
+			   const uint8_t hash[FIDO2_SHA256_SIZE], uint8_t *signature,
+			   size_t signature_size, size_t *signature_len);
+
+#ifdef __cplusplus
+}
+#endif
+
+#endif
